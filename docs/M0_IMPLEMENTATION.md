# Applicient — M0 Implementation Checklist

Working checklist for the M0 Foundation milestone. Check items off as they're done; update the **Status** line at each step when something changes. See [PRD.md](PRD.md) for the requirements and decisions this is built from.

**Status:** M0 complete. See [M1_IMPLEMENTATION.md](M1_IMPLEMENTATION.md) for the next milestone.

---

## 0. Repo bootstrap

- [x] Initialize the monorepo layout from PRD §8.1: `web/`, `api/`, `agents/`, `sources/`, `worker/`, `browser-worker/`, `scheduler/`, `renderers/`, `models/`, `metering/`
- [x] `.gitignore`, `.env.example` (never a real `.env`), `README.md` stub, `LICENSE` (MIT)
- [x] Pin toolchains: Python 3.12 (via `uv`) + `uv`, Node v24 + `pnpm`

## 1. Infra

- [x] `docker-compose.yml`: Postgres (pgvector extension), Redis, MinIO, placeholders for `api`, `worker`, `browser-worker`, `web`
- [x] Pinned image tags, each verified to actually exist before use (`pgvector/pgvector:0.8.6-pg16`, `redis:7.4-alpine`, `minio/minio:RELEASE.2025-09-07T16-13-09Z` — note: official minio/minio archived April 2026, this is the last verified official tag, fine for local dev per PRD §8.2)
- [x] Checkpoint: `docker compose up -d` → all three services report `healthy`
- [x] Checkpoint: `halfvec(2048)` HNSW index verified to build cleanly; plain `vector(2048)` verified to fail at index creation with the exact error the PRD predicted (`column cannot have more than 2000 dimensions for hnsw index`) — confirms `halfvec` is the correct, tested choice, not just a documented assumption

## 2. Database schema

- [x] Alembic migrations (hand-reviewed, not blindly autogenerated — see decision log below), first migration = every entity from PRD §9 (27 tables), every table carrying `user_id`
- [x] `EmbeddingIndex`/evidence/job embedding columns as `halfvec(2048)`, **not** `vector(2048)`
- [x] Seed data: one demo user (`demo@applicient.local`), empty unconfirmed profile shell — idempotent, verified safe to run twice
- [x] Checkpoint: HNSW indexes on both `halfvec(2048)` columns built via the actual migration path (not ad hoc SQL) and declared on the SQLAlchemy models so `alembic check` reports zero drift
- [x] Checkpoint: full round-trip verified — `upgrade head` → `downgrade base` → `upgrade head` on a real container, clean both directions

## 3. Models & cost ledger — build before any agent code

- [x] `ProviderConnection`, `ModelCatalogEntry`, `ModelProfile` tables (built in step 2) + Pydantic schemas (`api/src/applicient_api/schemas.py`)
- [x] OpenRouter adapter (default preset + embedding provider) — `list_models()` across both its chat and `/embeddings/models` catalogs, live pricing ingest
- [x] Anthropic adapter (quality reference preset) — bundled pricing table, since Anthropic's `/v1/models` doesn't return pricing
- [x] LangChain callback handler (`api/src/applicient_api/metering.py`) writing `LlmCall` on every call — verified against `langchain_openai`'s actual source for correct cache-token accounting, not assumed
- [x] Test-connection flow end to end: real OpenRouter key → connection created (encrypted) → tested (`status=ok`) → catalog populated (448 real models)
- [x] **M0 exit bar, verified — not via the GUI (that's step 5), but through the same underlying mechanism it will call:** real key in, real chat call out, `LlmCall` row lands with correct tokens/cost/latency. The literal "paste a key in the GUI" version of this test happens again in step 5 once the screen exists — this is the plumbing it will sit on top of.

## 4. Backend skeleton

- [x] FastAPI app: health check, REST CRUD for Profile/EvidenceItem (list/get/patch, list/create/delete), SSE endpoint for run streaming — all hit with a real `TestClient` and a real `uvicorn` process, not just import-checked
- [x] `agents/`: `deepagents` installed, simplest possible `create_deep_agent` call running standalone (no subagents, no tools) — real reply, first try
- [x] Tier-based model routing confirmed **live, through deepagents itself**: `resolve_tier(tier="fast")` → `ModelProfile.tier_bindings` → `ModelCatalogEntry` → a real `ChatOpenAI` instance, passed to `create_deep_agent(model=...)`. No model string anywhere in the agent code. A real `AgentRun` row ties to the real `LlmCall` row the call produced.

## 5. Frontend skeleton

- [x] Next.js 16 App Router + Tailwind v4 + shadcn/ui init (`web/`, pnpm) — genuinely newer than my training data (Next.js ships its own `AGENTS.md` warning about this now); read its bundled docs before writing route code rather than assume older App Router conventions
- [x] Ported the Terminal Ledger palette from `design/Tokens.dc.html` into `app/globals.css` — IBM Plex Sans/Mono via `next/font/google`, zero radius and no shadows as `@theme` tokens (not per-component), light/dark via shadcn's `.dark` class convention + `next-themes` (system/manual toggle) rather than the Artifact-specific `[data-theme]` contract, since this is a real app, not a published Artifact
- [x] Nav shell (rail + header) matching the mockup, all 8 routed pages from the design (`/radar`, `/inbox`, `/composer`, `/pipeline`, `/run-console`, `/events`, `/models`, `/cost`) — 7 are honest placeholders, `/models` is real
- [x] Models & Providers page wired to the real API — **first fully real screen**, verified against the actual live OpenRouter connection and 448-model catalog from steps 3-4, not sample data

## 6. CV ingest → evidence bank

- [x] PDF/DOCX parse → structured `Profile` JSON (`deep`-tier agent call)
- [x] Decompose into `EvidenceItem` rows, embed each via the `embedding` tier
- [x] Profile Studio confirm/edit UI

## 7. Close the M0 loop

- [x] Upload a CV → parsed profile → confirmed → evidence bank populated with embeddings → cost of that flow visible in a minimal Cost & Usage view
- [x] **M0 done**. M1 (first discovery adapter, dedup, scoring) starts next.

---

## Log

Short entries only — what changed, what's next, anything worth remembering. Newest first.

- **2026-08-20** — Post-M0 additions, raised by Adrian: (1) Cost & Usage's recent-calls table had no timestamp column — added one showing `LlmCall.created_at`. (2) Profile Studio had no way to edit the parsed candidate fields (name/headline/email/phone/location/links/summary/skills) or to wipe a profile and start over — the PATCH route already supported `parsed_profile` edits (schemas.py had it from the start), it just had no UI, so added an edit dialog; added `POST /profiles/{id}/reset`, which deletes the profile and everything hung off it (evidence items, personas, and transitively documents/fit scores/applications via FK cascade) and recreates a blank shell, since the app has no create-profile path and assumes exactly one profile per user — gated behind a type-to-confirm dialog given how destructive it is. (3) Multi-provider tier routing: confirmed each tier (`fast`/`balanced`/`deep`/`embedding`) already resolved its provider independently at the data-model level (`ModelProfile.tier_bindings` → per-entry `ModelCatalogEntry.provider_connection_id`, no cross-tier constraint) — the real gap was client construction only handling OpenAI-shaped providers, and the GUI only ever showing one connection's catalog at a time. Added a generic `openai_compatible` provider adapter (`providers/openai_compatible.py`) so any OpenAI-compatible endpoint (Gemini's OpenAI-compat endpoint, Groq, Together, a local server, etc.) can be connected with just a label + base_url + API key, no new adapter class per provider — the `base.py` interface was explicitly designed for exactly this. Fixed a latent bug found while wiring this up: `_build_chat_model`/`resolve_embedding_tier` defaulted any missing `base_url` to OpenRouter's URL regardless of provider, which would have silently sent a misconfigured `openai_compatible`/`openai` connection's calls to OpenRouter instead of erroring; also fixed `embed_evidence_items` inferring provider from a base_url substring check (mislabeling any non-OpenRouter OpenAI-compatible call as `"openai"` in the cost ledger) by threading the real `conn.provider` through explicitly instead. Reworked the Models & Providers tier-binding UI to source options from every tested connection's catalog, grouped by connection in each tier's dropdown, instead of only the one connection selected in the table — this is what actually makes "embedding on one provider, deep on another" pickable in the GUI.

  Verified live end to end, not just import-checked: created a real `openai_compatible` connection pointed at OpenRouter's own endpoint (a legitimate OpenAI-compatible target) using the existing OpenRouter key — `test_connection` returned `ok`, `refresh_catalog` returned all 415 real models via the generic adapter's `/models` parsing. Bound the `fast` tier to a catalog entry from this new connection while leaving `deep`/`balanced`/`embedding` bound to the original OpenRouter connection, then called `resolve_tier(tier="fast")` directly and invoked it — got a real completion, and the resulting `LlmCall` row correctly recorded `provider="openai_compatible"` (not the old buggy `"openai"`), proving both the cross-connection per-tier resolution and the provider-attribution fix work together against a live API. Test connection, its catalog entries, and the temporary tier binding were all cleaned up afterward and the original bindings restored. `tsc --noEmit`, `pnpm run lint`, and `pnpm run build` all clean.

  Anthropic's adapter still can't be bound to a chat-producing tier — `test_connection`/`refresh_catalog` work (it's cataloged), but `_build_chat_model` still has no `ChatAnthropic` branch (needs the `langchain-anthropic` dependency, not installed) and this work didn't add it, since the ask was generic OpenAI-compatible connectivity, not Anthropic specifically. Flagged here rather than left silently unfinished.

- **2026-08-20** — Post-M0 fix, raised by Adrian: `EvidenceItem` had `employer` and `text` but nowhere to put a job title, degree name, or certificate name — so extraction was jamming both halves of a source line like "Bangkit Academy — Machine Learning Cohort" into `employer`, losing the role/program name entirely. Added `EvidenceItem.title` (nullable `String(250)`, migration `62cd66038dbc`, round-tripped clean) — the specific role/degree/certificate/project name, kept distinct from `employer` (the organization). Updated the extraction schema and prompt with an explicit worked example forcing the split (field descriptions alone weren't reliable enough for this the same way they weren't for ISO dates back in step 6 — spelled it out in the prompt body too, not just the schema). Threaded `title` through the create/update routes, the embedding input (now `title\ntext\nSkills: ...`, so retrieval reflects the role context too — editing title now also invalidates and re-embeds, added to the same staleness-tracking trigger set as text/skills), and the Profile Studio UI, which now sub-groups evidence by `(employer, title)` instead of just `employer` — a promotion at the same company reads as two labeled blocks instead of one undifferentiated list, which was the whole point.

  Re-ran the exact CV that originally exposed this: `employer='Bangkit Academy'` / `title='Machine Learning Cohort'` and `employer='PT Fintek Nusantara'` / `title='Data Analyst Intern'`, cleanly separated on the live re-parse. Embeddings confirmed still generating correctly (`vector(2048)` on all 4 items) after the retrieval-input change.

- **2026-08-19** — M0 closed. CV ingest now persists a schema-validated structured profile JSON on the versioned `Profile`, replaces the active evidence bank only after parsing and embedding succeed, records an `embedded` readiness flag without exposing vectors, and marks failed runs as failed instead of leaving them stuck in `running`. Confirming a profile requires a non-empty, fully embedded evidence bank and propagates `verified` to every evidence row; edits invalidate confirmation and re-embed changed retrieval text.

  Added the missing fresh-install path in Models & Providers: the GUI can now save the active `deep`/`embedding` (and optional `fast`/`balanced`) tier bindings from a verified live catalog. The API validates ownership, provider status, embedding capability and structured-output capability before activating a preset. Profile Studio now shows the structured profile, embedding readiness, the confirmation gate, and an evidence editor.

  Added the `profiles.parsed_profile` migration and model-profile routes. Verified with `alembic upgrade head`, `alembic check`, TypeScript, ESLint, live FastAPI `TestClient` requests, an isolated real-provider CV upload/embedding flow against the real Postgres schema, a mocked upload/confirm flow, and an injected parser failure that correctly leaves its `AgentRun` failed. Temporary verification rows were removed. No commit was created.

- **2026-08-19** — Step 5 done. `web/` scaffolded with Next.js 16 (Turbopack, App Router) + Tailwind v4 + shadcn/ui (radix base, Nova preset as the starting point — fully overridden by our own tokens). Next.js 16 ships its own `AGENTS.md`/`CLAUDE.md` warning that it's newer than any training data and to read `node_modules/next/dist/docs/` before writing route code — did that before touching `layout.tsx`, confirmed the new `LayoutProps<'/route'>` global type helper is already what `create-next-app` generates by default.

  Terminal Ledger tokens ported from the design canvas as CSS custom properties in `globals.css`: exact hex values from `design/Tokens.dc.html` for both themes, IBM Plex Sans/Mono via `next/font/google` (confirmed present in Next's font-data.json before assuming the import names), zero radius and no shadows baked into the `@theme` block rather than left as a per-component discipline anyone could quietly break. Custom semantic tokens (`ok`/`warn`/`crit`/`evidence`) added alongside shadcn's defaults for the verdict badges and skill chips the mockups use throughout. Theming uses shadcn's `.dark`-class + `next-themes` convention (system-aware, manually overridable) — deliberately not the `[data-theme]`/`prefers-color-scheme` contract from the design-canvas skill, since that contract is for published Artifacts specifically and this is a real app with its own theme toggle.

  Nav shell + all 8 routes from the mockup's rail. Seven are honest placeholders (a header and "not built yet — see docs/IMPLEMENTATION.md", nothing pretending to be more finished than it is). `/models` is the real one: a `providers.py` FastAPI router (list/create/test/refresh-catalog/get-catalog, thin wrappers over step 3's `connections.py` service) plus CORS middleware, and a client-rendered page hitting it live — add-connection dialog, per-connection test/refresh actions, a catalog table with real prices and capability badges.

  Two real bugs, both from things I couldn't have caught without actually running them:
  - A newer, stricter `react-hooks/set-state-in-effect` ESLint rule flagged the standard fetch-on-mount pattern. Fixed by restructuring to the async-IIFE-with-cancellation shape (React docs' own recommended pattern for this) — and the restructure satisfied the rule on its own, no suppression needed in the end.
  - Port 3000 was already held by an unrelated `node` process on this machine (identified via `lsof`, left untouched — not mine to kill). Next.js auto-fell back to 3001, which silently broke CORS since the FastAPI allowlist only had 3000. Pinned the dev script to 3001 as a first fix; Adrian then freed port 3000 himself and asked to switch back, so reverted `web/package.json` to plain `next dev` — CORS allowlist keeps both 3000 and 3001 since either could recur on a different machine.

  **Honest gap, not silently glossed over:** no browser automation tool is available in this environment, so I could not visually confirm React hydration actually renders the fetched data — only verified up to the network boundary myself (SSR shell content, `tsc --noEmit` clean, `next build` clean with all 10 routes statically prerendered, and CORS headers + real JSON confirmed via `curl` with an `Origin` header simulating the browser's fetch). Asked Adrian to check `localhost:3000/models` in his own browser — confirmed real data rendering correctly, Terminal Ledger styling intact.

- **2026-08-19** — Step 4 done. The scope note from step 3's log resolved itself as predicted: `agents/` (new top-level `uv` package, `deepagents` + `langchain-openai`) needed the tier-resolution and metering logic built inside `api/` — added `applicient-api` as an editable path dependency rather than duplicating any of it. Flagging the resulting naming tension honestly rather than silently living with it: `api/` is now functionally the shared persistence/domain package (db, models, providers, metering, tier resolution) *and* about to also hold the FastAPI HTTP layer — the directory name suggests "just the HTTP layer" when it's really "the domain layer that also happens to expose HTTP." Not fixing this now (would mean touching everything built in steps 2-3 for a rename with no functional benefit); worth revisiting if it ever actually causes confusion rather than just looking slightly odd.

  Built `tier_resolution.py` (resolve a tier name to a real, ready-to-call `BaseChatModel`, tracing `ModelProfile.tier_bindings` → `ModelCatalogEntry` → decrypt the connection's key → construct `ChatOpenAI`) and `model_profiles.py` (the service function the Models & Providers GUI will call in step 5 to activate a preset — built now because tier resolution needs at least one active profile to resolve against).

  `agents/src/applicient_agents/setup_verification.py` is the actual step 4 proof: binds the `fast` tier to a real free OpenRouter model from step 3's catalog, creates a real `AgentRun`, resolves the tier through the chain above with **zero model strings anywhere in the agent code**, and calls `create_deep_agent(model=resolved_model)` — deepagents' actual public API, checked via `inspect.signature` before writing against it, not assumed. Worked on the first real run: got a reply, and the `LlmCall` the call produced is correctly tied to the `AgentRun` via `agent_run_id`. (Input tokens landed around 2,585 for a trivial prompt — that's deepagents' own middleware overhead, todo-list and filesystem tool descriptions baked into the system prompt even with no tools passed; expected, not a bug.)

  FastAPI app (`main.py` + `routers/`: health, profiles, evidence, streaming) with real CRUD against the DB built in steps 2-3 — verified with both a `TestClient` run (list/get/patch/create/delete/404, all against real rows) and a real `uvicorn` process actually answering HTTP requests on a port, not just import-checked. The SSE endpoint is a genuine stub, said so in its own docstring: it streams a real `AgentRun`'s status over real SSE, but nothing yet writes live `AgentStep` events for it to stream step-by-step — that lands once an agent run can be triggered through the API itself (M1+, once discovery/scoring agents exist to trigger).

  Demo profile got flipped to `confirmed=true` mid-verification (proving the PATCH route); reset back to its clean seeded state (`confirmed=false`) afterward so future dev work starts from the same baseline the seed script promises.

- **2026-08-19** — Step 3 done. Scope note logged up front: PRD architecture wants `models/` and `metering/` as standalone top-level packages so `worker/`/`agents/` can import them independently later. Built inside `api/` for now (`api/src/applicient_api/providers/`, `metering.py`, `connections.py`, `security.py`, `schemas.py`) since nothing else needs them yet — promoting to separate packages is a mechanical move once `agents/` (step 4) actually needs the cross-package import, not a design change.

  **API shapes verified against the live services before writing any parsing code, not assumed:**
  - OpenRouter: `GET /api/v1/key` (Bearer auth) is the only endpoint that validates a key — `/api/v1/models` is public and returns 200 regardless. Embedding models live in a completely separate catalog, `GET /api/v1/embeddings/models` — `nvidia/nemotron-3-embed-1b:free` does not appear in the regular `/models` listing at all. Neither listing returns a vector dimension (not provider-discoverable; has to be known out of band per model).
  - Anthropic: `GET /v1/models` needs `x-api-key` + `anthropic-version` headers and validates the key, but returns no pricing — bundled a pricing table from the same source Claude Code's own `claude-api` skill uses, stamped as `pricing_version`. An unrecognized model id still lists, just with `pricing_known=False`.
  - `langchain_openai`'s actual source (not the docs) confirmed `usage_metadata.input_tokens` already **includes** `cache_read` as a subset — read straight from `prompt_tokens_details.cached_tokens`, not subtracted separately. Getting this backwards would have double- or under-billed every cached call. `cache_creation`/reasoning-token handling is documented as an assumption in `metering.py` since nothing exercises prompt caching yet to verify it live.

  **One real bug only the live API surfaced:** OpenRouter's own meta-router models (`openrouter/auto`, `.../fusion`, `.../pareto-code`, `.../bodybuilder`, `.../auto-beta` — confirmed all five, not a one-off) report pricing as `"-1"` per token, a sentinel for "depends on the model actually picked," not a literal price. `-1 * 1e6` overflowed the `Numeric(12,6)` cost column and the catalog refresh failed outright on the very first live run. Fixed in the adapter: negative prices → `pricing_known=False`, never a stored negative cost. No amount of stubbing would have caught this — only hitting the real endpoint did.

  **The M0 exit bar is proven, live, end to end:** created a real encrypted `ProviderConnection` with Adrian's OpenRouter key, tested it (`status=ok`), refreshed the catalog (448 models, nemotron embed present, the pricing sentinel handled), then made one real chat completion through `langchain_openai` with the cost-ledger callback attached — an `LlmCall` row landed with correct `input_tokens`/`output_tokens`, `cost_usd=0` + `cost_known=True` (free model), and real latency. The GUI version of this same test happens again in step 5; this proved the mechanism underneath it works.

  One disclosure: the temporary `OPENROUTER_API_KEY_SETUP_TEST` line Adrian added to `.env` for this test was read back in full by a file-read tool call while removing it afterward (needed the exact content to edit precisely) — so the raw key appeared once in this session's tool output, never in a reply. Flagged to Adrian; his call whether to rotate it, not urgent for a local session. `.env` is confirmed clean of it now.

- **2026-08-19** — Step 2 done. Discussed Alembic vs. alternatives first — Adrian's past experience of "migrations make the DB slow" was actually migration-time locking (blocking `ALTER`/`CREATE INDEX` on live tables, or naive autogenerate output), not Alembic itself, which has zero runtime footprint. Agreed: keep Alembic, but hand-review every migration rather than trust `--autogenerate` output as final.

  Built the full SQLAlchemy schema in `api/src/applicient_api/models/` (9 files, 27 tables matching PRD §9 — enums stored as `VARCHAR`+`CHECK` rather than native Postgres `ENUM`, specifically because altering a native enum's values is exactly the kind of migration pain that started this conversation). UUID PKs via Postgres's built-in `gen_random_uuid()` (verified available with no extension needed on this image). Consistent constraint naming convention set on `Base.metadata` so future autogenerate diffs stay stable/reviewable.

  Two real bugs caught by actually running things rather than trusting them:
  - `pgvector-python`'s SQLAlchemy type is `HALFVEC`, not `HalfVector` as I initially guessed — caught by inspecting the installed package before writing model code.
  - Autogenerate emits fully-qualified `pgvector.sqlalchemy.halfvec.HALFVEC` references in migration files but doesn't add the import — `alembic upgrade` fails with `NameError` until you add it by hand. Now a one-line comment in the migration explaining why.

  One environment issue, not a code bug: `localhost:5432` was silently hitting a pre-existing native Homebrew `postgresql@14` service on this machine, not the Docker container — same host, two processes both bound to loopback on that port, macOS resolved to the native one first. Did not touch the existing service; remapped the container to host port **5433** instead (`docker-compose.yml`, `.env`, `.env.example` all updated, commented in the compose file so this doesn't get "fixed" back to 5432 later without knowing why).

  Verified the HNSW/`halfvec` claim from step 1 all the way through the real migration path this time (not just a scratch table): declared the indexes on the `EvidenceItem`/`Job` models with `postgresql_using="hnsw"`, confirmed `alembic check` reports **zero drift**, and round-tripped `upgrade head → downgrade base → upgrade head` cleanly on the live container.

  Seed script at `api/src/applicient_api/seed.py` (`uv run python -m applicient_api.seed`) creates one demo user + empty profile shell, confirmed idempotent.

  `db.py` and `migrations/env.py` both load `.env` from the repo root independently (each entry point — scripts, alembic, eventually the FastAPI app — needs it, and there's no single process boundary yet to load it once centrally).

- **2026-08-19** — Steps 0-1 done. Repo scaffolded (dirs, `.gitignore`, `.env.example`, README, MIT LICENSE, `api/` initialized with `uv`). Toolchain installed: `pnpm`, `uv`, Python 3.12.14 pinned via `uv` (system default is 3.14 — deliberately not using it, LangChain/ML deps can lag brand-new Python versions). `docker-compose.yml` up with Postgres/pgvector, Redis, MinIO — all pinned tags verified to exist on the registry before use (one guessed MinIO tag was caught wrong and corrected this way). Confirmed `halfvec(2048)` HNSW builds and plain `vector(2048)` fails exactly as the PRD predicted. GitHub remote already existed (`AdrianRamadhan27/applicient`, empty, `main`) — noted a gh-account/git-identity mismatch to watch at first push, but not resolved (user will handle). Adrian has an OpenRouter key ready for step 3. **Per user instruction: Claude does not run `git commit` in this repo — files are staged/created, commits are the user's.**
- **2026-08-19** — Checklist created. Nothing implemented yet.

